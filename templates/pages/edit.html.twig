{% extends 'base.html.twig' %}

{% block title %}{{name_site}}{% endblock %}

{% block body %}
    <h1>Edit Pages</h1>

    {{ include('pages/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_pages_index') }}">back to list</a>

    {{ include('pages/_delete_form.html.twig') }}
{% endblock %}
